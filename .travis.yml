language: python
python:
  - "3.6"
  - "3.7"
  # - "nightly"
# command to install dependencies
install:
  - pip install -r requirements.txt
# command to run tests
script:
  - inv test
sudo: true
notifications:
  email:
    - jima80525@gmail.com
  on_success: always
  on_failure: always


